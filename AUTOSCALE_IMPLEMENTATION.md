# 自动缩放功能实现说明

## 概述
已实现了图像显示的自动缩放功能，使得图像能够随着整体窗口大小变化而自动调整，始终以最大可能的尺寸显示。

## 实现的功能

### 1. 加载图像后的自动缩放
当在"图像处理"标签页加载图像后，图像可以随着窗口大小改变而自动缩放至可显示的最大值。

**实现细节：**
- 修改了 `ImageDisplay` 类
- 添加了 `<Configure>` 事件监听，用于捕捉窗口大小变化
- 在大小改变时自动调用 `_update_display()` 方法重新缩放图像

### 2. 前景分割中的自动缩放
在"前景分割"标签页中的两种显示方式（普通交互和GrabCut迭代）都支持自动缩放：

**交互式图像显示（InteractiveImageDisplay）：**
- 添加了 `<Configure>` 事件监听和 `_on_frame_configure()` 方法
- 在标注和编辑图像时保持最优显示尺寸
- 维持坐标映射的正确性（鼠标操作与实际图像对应）

**GrabCut迭代显示（IterativeGrabCutDisplay）：**
- 添加了 `<Configure>` 事件监听和 `_on_frame_configure()` 方法
- 在标注过程中自动调整显示尺寸

### 3. 图案生成中的自动缩放
在"图案生成"标签页中：

**自动渲染：**
- `PatternDisplay` 继承并使用 `ImageDisplay` 类
- 自动获得了窗口大小变化时的缩放功能
- 生成图案、带网格渲染、带编码渲染的结果都会自动适应窗口大小

## 技术细节

### 修改的文件
- `src/ui/main_window.py`

### 修改的类
1. **ImageDisplay** (基础图像显示)
   - 添加了 `last_width` 和 `last_height` 属性用于追踪窗口大小
   - 添加了 `_on_frame_configure(event)` 方法
   - 在 `__init__` 中绑定 `<Configure>` 事件

2. **InteractiveImageDisplay** (交互式图像显示)
   - 添加了 `last_width` 和 `last_height` 属性
   - 添加了 `_on_frame_configure(event)` 方法
   - 在 `__init__` 中绑定 `<Configure>` 事件

3. **IterativeGrabCutDisplay** (GrabCut迭代显示)
   - 添加了 `last_width` 和 `last_height` 属性
   - 添加了 `_on_frame_configure(event)` 方法
   - 在 `__init__` 中绑定 `<Configure>` 事件

### 缩放算法
缩放算法保持了原有的特性：
- 图像自动适应可用空间，同时保持纵横比
- 只在必要时缩小图像（不会放大模糊）
- 图像在显示区域中心对齐

### 性能优化
为了避免过度重绘，添加了大小变化检测：
- 只有当窗口大小实际改变时，才会触发 `_update_display()`
- 使用 `last_width` 和 `last_height` 属性防止重复处理相同的大小

## 测试建议

1. **加载图像并调整窗口大小：**
   - 在"图像处理"标签页加载一张图像
   - 调整主窗口的大小，观察图像是否自动缩放

2. **交互式标注时的缩放：**
   - 在"前景分割"标签页进行交互式标注
   - 调整窗口大小，确保图像缩放正常且鼠标坐标映射仍然准确

3. **生成图案时的缩放：**
   - 在"图案生成"标签页生成图案
   - 进行渲染操作，观察结果是否能自动适应窗口大小

## 注意事项

- 缩放功能不会改变原始图像数据，只是调整显示尺寸
- 所有坐标映射和交互操作都已正确调整，以适应缩放后的显示
- 性能影响很小，因为重绘只在实际需要时进行

## 已完成的功能清单

✅ 加载图像后的自动缩放  
✅ 窗口大小变化时的自动重新缩放  
✅ 交互式标注程序中的自动缩放  
✅ GrabCut迭代分割中的自动缩放  
✅ 图案生成结果的自动缩放  
✅ 坐标映射的正确性维持  
✅ 性能优化（避免不必要的重绘）
