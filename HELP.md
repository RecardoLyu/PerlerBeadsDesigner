# 拼豆图纸设计器 · 使用说明

> 本文档介绍「拼豆图纸设计器」(Perler Beads Designer) 的整体使用流程。
> 每次功能更新都会同步维护本文档，请与软件版本对照阅读。

---

## 目录

- [一、快速上手](#一快速上手)
- [二、完整工作流程](#二完整工作流程)
  - [1. 加载图像](#1-加载图像)
  - [2. 图像预处理（变换 / 裁剪 / Mask 抠图 / 分割）](#2-图像预处理变换--裁剪--mask-抠图--分割)
  - [3. 生成拼豆图纸](#3-生成拼豆图纸)
  - [4. 导出预览与导出选项](#4-导出预览与导出选项)
  - [5. 一键导出](#5-一键导出)
- [三、导出文件说明](#三导出文件说明)
- [四、Mask（抠图）与 BOM 用量统计](#四mask抠图与-bom-用量统计)
- [五、颜色表（色板）](#五颜色表色板)
- [六、常见问题 FAQ](#六常见问题-faq)
- [七、参数调优建议](#七参数调优建议)

---

## 一、快速上手

**启动方式**

- Windows 可执行版：双击 `PerlerBeadsDesigner.exe`。
- 源码运行：在项目根目录执行
  ```bash
  python -m src.webapp.main
  ```

**界面布局**

顶部菜单栏下方的标签页对应工作流程：

| 标签页 | 作用 |
|--------|------|
| **图像处理** | 图像变换（旋转/翻转）、裁剪、基本调整、Mask 抠图、分割 |
| **图纸生成** | 设置图纸尺寸并生成图纸、导出预览、导出选项与一键导出 |

左侧是参数与操作区，中间是图像/图纸预览区，右侧（图纸生成页）是 BOM 物料清单。

> 标签页行右上角的 **❓ 帮助** 按钮（与「图像处理」「图纸生成」标签同行）可随时打开本说明文档（内嵌于软件中）。

**最小流程（5 分钟）**

加载图像 → （可选）预处理抠图 → 「图纸生成」点 **生成图纸** → 在「导出选项」勾选格式点 **一键导出**。

---

## 二、完整工作流程

### 1. 加载图像

在「图像」标签页点击 **加载图像**，选择本地图片（JPG / PNG / BMP 等常见格式）。

- 建议原图分辨率适中即可，软件会按你设定的拼豆数自动缩放。
- 加载后窗口尺寸保持不变，图像自适应显示区。

### 2. 图像预处理（变换 / 裁剪 / Mask 抠图 / 分割）

「图像处理」标签页内含子标签：**基本调整** 与 **分割**。

**基本调整**
- **图像变换**：旋转（↺90° / ↻90° / 180°，或输入任意角度点「旋转」）与翻转（**水平对称** / **垂直对称**，按钮带对称图标）。变换只作用于当前工作图，**恢复原图** 可回到最初未变换的状态。
- **裁剪**：点 **裁剪**，按钮会换成较小的 **取消 / 应用** 两个按钮（颜色区分）。在画布上 **左键拖框** 选取保留区域；框选后可 **拖动边框/四角手柄** 微调、**拖动框内** 整体移动（带轻微吸附，框外误点不会重新框选）。**放大后想平移视角**：按住 **鼠标中键拖动** 整体移动 ROI，左键拖动才是改裁剪框。点 **应用** 生效 —— 裁剪后这张图就成为新的「原图」，**恢复原图** 也只回到裁剪后，不会回到裁剪前；但 **重新导入** 可以。
- 高斯模糊、亮度/对比度等基础调整（按需使用）。鼠标悬浮「高斯模糊」标题或核大小输入框可查看核大小说明。
- **恢复原图**：撤销所有预处理，回到最初加载的图像（裁剪后的状态）。

> **子交互防护**：进入任何新操作（点裁剪、点分割框选/涂抹、切换标签页）会 **自动取消上一个未完成的操作**。例如裁剪框选中途点了「开始框选」，裁剪框会立即消失回到浏览态，避免状态串扰。

**分割 / Mask 抠图**（用于去掉背景）
- **迭代 GrabCut**：先选前景区域形状（**矩形** / **椭圆**（按住 Shift 为正圆）/ **自由曲线**）。框选与首次分割已合并为**一个按钮**：点 **开始框选** 进入框选态，在图像上拖出前景范围后按钮自动变为 **进行分割**，再点即执行首次 GrabCut 分割（框选中再点一次可取消）。→ 用 **前景 / 背景** 涂抹修正（涂抹**实时**显示，前景为**红色**、背景为**绿色**；下方 **笔触** 滑块可调整涂抹粗细，前景/背景旁的**彩色圆点**实时显示真实笔触尺寸，红色=前景、绿色=背景）→ **迭代分割** → **应用分割结果**。
- **自动分割**：无需手动圈选，一键分割。可在下拉框中选择算法，**每种算法下方会显示对应的可调参数**：
  - **GrabCut(矩形初始化)**：以居中矩形为初值做图割，主体居中时效果好（默认）。参数 **矩形边距**：初始矩形相对图宽的留白比例，越小框越大。
  - **分水岭 Watershed**：自动种子分水岭，边缘贴合，适合轮廓清晰的主体。参数 **前景比例**：确定前景的距离阈值比例，越大前景越保守。
  - **Otsu 自适应阈值**：一键快速二值化，适合主体与背景明暗对比强。全自动，无需参数。
  - **SLIC 超像素**：按颜色聚成小块再聚合，适合平涂/卡通素材，边缘整齐。参数 **超像素数**：聚类块数，越大边缘越细、越慢。
  - **分割结果可视化**：执行分割后默认显示 **原图+高亮**（在原图上叠加**橙色半透明**高亮标出前景）。顶部「显示」下拉可在 **原图 / 原图+高亮 / Mask / Mask应用结果** 之间切换。
  - 点 **应用分割结果** 会把 Mask 直接作用在原图上，在右侧窗口显示抠图结果（**前景保留彩色，背景变为纯白**）。
- **形态学操作**（加粗标题）：对分割 Mask 做开/闭运算、腐蚀/膨胀；结构元素下拉中名称与示意图记号对齐排列，鼠标悬浮「结构元素」标签或下拉框可查看各形状的方向/轮廓说明。**形态学结果会同步进最终抠图**：修改后的 Mask 在点「应用分割结果」时生效（迭代 GrabCut 与自动分割的 Mask 都会随之更新）。
- 点 **应用分割结果**（迭代 GrabCut 或自动分割）会把 Mask 直接作用在原图上，在右侧窗口显示抠图结果（**前景保留彩色，背景变为纯白**）。
- 抠图后，到「图纸生成」勾选 **使用 Mask 处理结果** 再生成图纸，背景就不会参与拼豆。
- 分割计算在**后台线程**执行：运行期间窗口保持响应，可随时点击 **加载图像** 打断当前分割（旧结果会被自动丢弃，不会覆盖新图）。

> Mask 的详细行为见 [第四节](#四mask抠图与-bom-用量统计)。

### 3. 生成拼豆图纸

切换到「图纸生成」标签页，设置参数后点 **生成图纸**。图纸尺寸（高 / 宽的拼豆粒数，默认 52×52）在 **图纸尺寸** 区设置：

| 参数 | 说明 |
|------|------|
| **图纸尺寸（高 / 宽）** | 图纸的拼豆行数 / 列数（粒数）。改一个会按原图宽高比自动联动另一个。 |
| **品牌（豆子颜色库）** | 选择拼豆品牌，决定色号与颜色。内置 5 个品牌：**MARD 曼德**（221 色，中文名）、**Perler**（103 色）、**Hama**（92 色）、**Artkal S-5mm**（199 色）、**Artkal C-2.6mm**（174 色）。切换后旧图纸失效，需重新生成；图纸色号会严格对应该品牌。 |
| **颜色数量** | 限制使用的豆子颜色数，`0` = 不限制（用全部所需颜色）。限制越低越省色但可能丢细节。上限随所选品牌的色数自动调整。 |
| **细节保留** | 限制颜色时，保留稀有细节色（如眼睛、边缘）的强度。 |
| **抖动 (Floyd-Steinberg)** | 用误差扩散在低色数下模拟渐变层次；**抖动强度** 控制程度。 |
| **高阶优化 (ICM 空间相干)** | 让相邻格颜色更连贯、减少噪点，同时保留高显著性细节。 |
| **使用 Mask 处理结果** | 勾选后用图像处理抠图结果生成（背景不参与）。需先在图像处理执行 Mask 应用。 |

生成成功后状态栏显示 `图案生成成功: 宽x高, 使用N种颜色`，右侧 BOM 列表更新。

> **图纸顶部信息**：左上角始终显示 **品牌 · 颜色数 · 总豆数** 紧凑单行，方便对照实物购买。**文件名标题**默认**不**画在图纸上——在「导出」区勾选 **在图纸上显示文件名** 后，才把导出文件名居中、粗体画到图纸顶部。

### 4. 导出预览与导出选项

「图纸生成」页的 **导出选项** 区（原"渲染选项"位置）提供：

- **导出预览** 复选框（原"带编码渲染"）：切换两种预览 —
  - **不勾选（网格预览）**：纯拼豆色块 + 网格线，看整体配色。
  - **勾选（标准图纸）**：正式图纸版式 —— 每格标色号、每 5 格加粗主线并带左/上坐标刻度（5、10、15…），底部 **BOM 用量条**。预览与导出文件完全一致（所见即所得）。
- **导出参数**：文件名、PNG 缩放倍数、PDF 页面大小（A4 / Letter）。
- **导出格式**（按需勾选）：导出 PNG、导出 PDF。
- **一键导出** 与 **选择输出路径**。

### 5. 一键导出

在「图纸生成」页的 **导出选项** 区：

- 填好 **文件名**（默认跟随源图文件名，未加载图时为 `pattern`）、PNG 缩放倍数、PDF 页面大小。旁边的 **在图纸上显示文件名** 复选框（默认不勾）控制是否把该文件名画到图纸顶部标题。
- 勾选需要的 **导出格式**（PNG / PDF）。
- 点 **一键导出**，文件保存到设定的输出目录（默认项目 `output/`）。

---

## 三、导出文件说明

勾选 PNG / PDF 后会生成以下文件（均放入 `output/`）：

| 文件 | 内容 |
|------|------|
| `文件名_图纸.png` | **标准图纸**：色块 + 每格色号 + 粗细分网格 + 坐标刻度 + 底部 BOM 用量条。 |
| `文件名.pdf` | 把标准图纸排版进 A4/Letter 页面，自动适配缩放，可直接打印。 |

> 自 v1.8.1 起不再单独导出无编码的 `_grid` 网格版 PNG —— 网格配色直接在软件「图纸生成」页预览即可，导出统一为标准图纸。

> 物料用量信息（每色编号 / 名称 / 数量 / 百分比）直接展示在软件右侧 BOM 列表与图纸底部的 BOM 用量条中。

**标准图纸的 BOM 用量条**：每个颜色是一枚统一的圆角矩形芯片 —— 左侧色块标注**色号**（深色豆子用白字、浅色用黑字），右侧标注**数量**，按用量从多到少排列成整齐的多行网格。

---

## 四、Mask（抠图）与 BOM 用量统计

当你用 Mask 抠掉背景后，图纸和 BOM 会做如下处理（**仅当勾选了「使用 Mask 处理结果」**）：

1. **图纸显示**：被 Mask 屏蔽的背景区域会以**淡化**（向白色减淡）方式呈现，且这些格子**不标注色号**，让主体清晰突出、背景仅供定位参考。
2. **前景与 Mask 一一对应**：所有保留的前景格子**都会标注色号**，不会出现"有颜色却没编号"的情况。边缘格按"非白即前景"规则判定 —— 只要某格量化后是明显非白的真实颜色，就一律归入前景并标注编号。
3. **BOM 用量统计**：**背景区域不计入用料**。图纸底部 BOM 用量条、软件右侧 BOM 列表，都只统计保留下来的前景豆子数量 —— 不会出现"背景色几百粒"的误导数据。

> 也就是说：抠图后导出的 BOM 就是你真正需要购买的豆子数量。
> 预览与导出的标准图纸使用同一渲染结果，背景淡化在预览和导出文件中一致生效。

---

## 五、颜色表（色板）

- 软件内置 **5 个拼豆品牌** 的标准色板，在「图纸生成」页的 **品牌** 下拉中选择：
  | 品牌 | 色数 | 说明 |
  |------|------|------|
  | **MARD 曼德** | 221 | 默认，中文色号名 |
  | **Perler** | 103 | 经典美式品牌 |
  | **Hama** | 92 | 欧洲常用，色号以 `H` 开头 |
  | **Artkal S-5mm** | 199 | 5mm 大豆 |
  | **Artkal C-2.6mm** | 174 | 2.6mm 小豆 |
- 生成图纸时，软件会把图像颜色匹配到所选品牌中最接近的实体豆子色号（采用 CIEDE2000 感知色差，更贴近肉眼）。
- 色板数据存于 `src/assets/palette/<品牌>.json`，如需自定义可编辑对应文件（保持字段格式一致）。
- 切换品牌后旧图纸失效，需重新生成；图纸顶部会标注当前品牌与总豆数。

---

## 六、常见问题 FAQ

**Q：为什么导出的 BOM 里没有背景色？**
A：这是正常且期望的行为。使用 Mask 抠图后，背景区域不纳入用料统计（见第四节）。若未使用 Mask，则整图所有豆子都会计入。

**Q：导出的 PDF / 图纸模糊、编号看不清？**
A：当前版本已提升图纸渲染分辨率并采用更清晰的字体与抗锯齿（超采样）处理，编号清晰。如需更大字，可增大 PNG 缩放倍数导出。

**Q：PDF 里中文显示成方框？**
A：软件会自动注册系统中文字体（微软雅黑 / 黑体 / 宋体）。请确认系统安装了这些字体（Windows 默认都有）。

**Q：颜色数量限制设多少合适？**
A：见下节调优建议。一般卡通/Logo 可设较少（如 8–16），人像/风景建议不设限或设较高值，并配合抖动。

**Q：不同品牌的色号对不上 / 想换豆子品牌？**
A：在「图纸生成」页的 **品牌** 下拉选择你手头豆子的品牌（MARD / Perler / Hama / Artkal S / Artkal C），重新生成图纸即可，图纸色号与 BOM 会严格对应该品牌。图纸顶部也会标注当前品牌与总豆数，方便对照购买。

**Q：裁剪后「恢复原图」为什么没回到裁剪前？**
A：这是设计行为。裁剪「应用」后，裁剪结果即成为新的原图（便于在抠出的主体上继续处理）；想回到最初只能 **重新导入** 图片。

**Q：裁剪/框选/涂抹中途点了别的按钮，原来的操作呢？**
A：软件会自动取消上一个未完成的子交互（防护机制），避免状态串扰。重新点对应按钮即可重新开始。

**Q：窗口被图像撑大 / 显示异常？**
A：当前版本已修复窗口尺寸稳定性，加载任意尺寸图像窗口都保持不变。

---

## 七、参数调优建议

| 题材 | 颜色数量 | 抖动 | ICM | 说明 |
|------|----------|------|-----|------|
| 卡通 / Logo / 像素画 | 8–16 | 关 | 可开 | 色块分明，限制低色数更干净。 |
| 人像 | 不限或 24–48 | 开（中强度） | 开 | 保留肤色渐变，ICM 减少脸部噪点。 |
| 风景 / 照片 | 不限 | 开 | 开 | 色彩丰富，抖动表现渐变层次。 |
| 简单图案 / 文字 | 4–8 | 关 | 关 | 极少量颜色即可。 |

**拼豆尺寸（宽高粒数）**：越大细节越丰富但用料与工时越多。常见挂件 30–60 粒边长即可，大型作品可到 100+。

---

*本文档随软件版本持续更新。*
