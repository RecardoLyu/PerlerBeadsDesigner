# 功能测试单 (TESTING)

本文件记录**待人工验证的功能测试项**，按版本分组。打勾表示已通过。
自动化测试（`pytest` / `flutter test` / `flutter analyze`）不列于此——它们随每次构建自动跑；这里只记**需要在真实设备/界面上人工确认**的项。

---

## v2.5.0

> 版本内容：图纸画板（双端）· 图像转换整合 · 风格更名「真实风/图纸风」· 安卓更新系统重做（后台下载/镜像回退/断点续传/正式签名）。
> 桌面端以下 A/B/D 组已在本机浏览器自动化验证通过（发版时），此处保留供复测。

### 0. 准备

- [ ] 安卓端安装 `PerlerBeadsDesigner-android-v2.5.0.apk`
  > ⚠️ 若设备上是 **v2.4.1 或更早**（旧 debug 签名），因 v2.5.0 起改用正式 release 签名，**首次需先卸载旧版再装**；此后版本即可正常覆盖安装。
- [ ] 桌面端解压运行 `PerlerBeadsDesigner-windows-v2.5.0.zip`（或对应平台包）

### A. 风格更名「真实风 / 图纸风」（双端）

- [ ] 豆子风格选项显示「真实风」「图纸风」，不再出现「真实豆子 / 经典方格」
- [ ] 切换两种风格，图纸渲染正确（真实风=同心圆环+中央孔洞；图纸风=方格印色号）
- [ ] 默认优先真实风；导出图纸的风格与当前显示一致
- [ ] 设置里可改默认风格并持久化（重开仍生效）
- [ ] 双端（桌面 / 安卓）措辞一致

### B. 桌面端「图像转换」整合

- [ ] 顶部只剩「图像转换」「图纸画板」两个入口
- [ ] 左栏收起按钮旁有「图像调整」「图纸生成」两个子切换
- [ ] 点「图像调整」= 原「图像处理」全部功能（加载图像 / 亮度对比度高斯模糊 / 裁剪 / 分割 / 形态学）
- [ ] 点「图纸生成」= 原图纸参数 + BOM + 导出，功能不丢
- [ ] 两个子切换互斥、正确替换左栏参数卡片，画布标题随之变化
- [ ] 切到「图纸画板」时左栏变为画板工具栏

### C. 移动端导航整合

- [ ] 底部三入口：图像调整 / 图纸生成 / 图纸画板，图标正常
- [ ] 「图像调整」整合原「调整 + 分割」全部功能
- [ ] 「图纸生成」整合原「图纸 + 导出」全部功能
- [ ] 切到「图纸画板」时主预览区变成画板画布
- [ ] 设置入口仍在右上角

### D. 图纸画板（桌面端）

- [ ] 新建画板可选 52×52 / 104×104，默认 52
- [ ] 豆色以「圆环 + 编号」呈现，对接所选品牌豆库，选色正确
- [ ] 笔触粗细滑块以拼豆板像素为单位调节（1–3）
- [ ] 工具：画笔涂抹、油漆桶填充、橡皮擦、自由框选，各自行为正确
- [ ] 撤销 / 重做各保留 5 步，恢复正确
- [ ] 导入外部图像作底图：切成正方形铺满画板，默认高透明度叠加、不干扰绘制，可开关 / 调透明度，可清除
- [ ] 导出 PNG：可选真实风 / 图纸风，导出参数与「图像转换」导出条目一致；生成色号 BOM
- [ ] 交互：左键绘制 / 涂抹，中键拖动平移，滚轮缩放；画布默认真实拼豆渲染
- [ ] 画板状态下显示「真实风 / 图纸风」切换

### E. 图纸画板（移动端）

- [ ] 工具 / 颜色等放在「屏幕下方」参数栏：底部紧凑工具条 + 横滚颜色条（圆环 + 编号）
- [ ] 单指点击选色 / 选工具、点击或涂抹绘制
- [ ] 双指捏合缩放、双指拖动平移，与图像画布交互手感一致
- [ ] 导入底图：自动中心正方形裁剪 + 高透叠加 + 可开关 / 调透明度
- [ ] 撤销 / 重做 5 步；填充 / 橡皮 / 框选正常
- [ ] 导出 PNG（真实风 / 图纸风）+ 色号 BOM，导出参数与移动端现有一致

### F. 安卓在线更新（重点，本次重做）

- [ ] 设置 → 关于 → 检查更新：能检测到新版本（下次发版时验证；当前已最新则显示「已是最新」）
- [ ] GitHub 直连失败时自动回退公共镜像加速源（弱网/断 GitHub 时观察）
- [ ] **后台下载**：下载中途退出设置页，系统通知栏仍在下载；重进设置页，进度 / 状态正确恢复
- [ ] **断点续传**：下载到一半断网，网络恢复后能接着下，不从头重来
- [ ] 下载文件有明确存储路径与状态提示（通知栏进度 + 页面提示）
- [ ] **覆盖安装**：v2.5.0 → 后续版本（同 release 签名）可正常覆盖安装成功
  > 注：v2.4.1 → v2.5.0 因签名变更需先卸载（一次性），属预期。
- [ ] **下载完整性校验（v2.5.0 补丁新增）**：下载完成先校验（ZIP 魔数头 + 字节数比对 GitHub asset.size + 可选 sha256），**坏包不再交给安装器**，自动换镜像重下；全镜像都坏时提示「安装包损坏」而非系统报「软件包似乎无效」
  > 模拟坏包验证：往 `Android/data/com.recardo.perler.perler_app/files/updates/` 放一个截断/HTML 假 APK，触发下载完成，应被拦下并自动重试。
  > 真机核对失败包：`sha256sum <apk>`（v2.5.0 应 = `7f440c823bc00b3c8edcc74bd1ec466ce9c02f03455ca8f717ffb2942c17ee00`）、字节数应 = 149277399、`head -c4 <apk>` 应为 `PK`。

### H. 生成 / 导出交互反馈（v2.5.0 补丁新增）

- [ ] 加载一张较大图像，点「生成图纸」：按钮**立刻**变「生成中…」并禁用，颜文字开始跳动、状态灯变红，**无卡死感**；算完按钮恢复、灯变绿、图纸出现
- [ ] 开「高阶优化(ICM) + 抖动」（最重负载）再点生成：全程 UI 仍可响应、反馈持续
- [ ] 点「导出 PNG 到相册」：导出中颜文字跳动、灯变红、按钮「导出中…」，完成后恢复并提示已导出
- [ ] 生成 / 导出期间可正常切换 Tab、拖动画布（不再被主线程阻塞）

### G. 回归测试

- [ ] 核心流程不回归：加载图像 → 预处理 → 分割 → 生成图纸 → 导出 PNG（桌面 / 安卓各过一遍）
- [ ] 真实风 / 图纸风两种图纸的 BOM 统计正确
- [ ] 自定义主题（换肤）功能不受影响
- [ ] 桌面端打包（PyInstaller）/ 安卓 release 构建均正常

---

## 测试记录

| 日期 | 版本 | 测试人 | 范围 | 结果 | 备注 |
|---|---|---|---|---|---|
| 2026-08-05 | v2.5.0 | （自动） | 桌面 A/B/D + 构建 G | ✅ 通过 | 浏览器自动化 + CI 四端 + 签名指纹核对 |
| | | | | | |
